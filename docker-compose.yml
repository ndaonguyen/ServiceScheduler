# Local development dependencies. Start with: docker compose up -d
# The API's default connection string (appsettings.json) targets this instance.
services:
  postgres:
    image: postgres:17-alpine
    container_name: servicescheduler-postgres
    environment:
      POSTGRES_DB: servicescheduler
      POSTGRES_USER: servicescheduler
      POSTGRES_PASSWORD: servicescheduler
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      # Schema is created by EF Core migrations (the API auto-migrates in Development;
      # otherwise run `dotnet ef database update`). No init scripts here.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U servicescheduler -d servicescheduler"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
